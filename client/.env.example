# Configuración de desarrollo para Vite
# Copiar este archivo a .env y personalizar según necesites

# Puerto del servidor de desarrollo (default: 5173)
# Ejemplos: 3000, 8080, 5000
VITE_PORT=5173

# Puerto para preview build (default: 4173)
VITE_PREVIEW_PORT=4173

# Host del servidor (default: localhost)
# Usar 0.0.0.0 para acceder desde otros dispositivos en la red local
VITE_HOST=localhost

# Nota: Estas variables se usan en vite.config.ts para configurar
# el servidor de desarrollo. NO son expuestas al código del cliente.
