# Configuración para Docker Compose
# Copiar este archivo a .env antes de ejecutar docker-compose up

# Puerto de la aplicación (mapeado desde el host)
APP_PORT=3000

# Modo de ejecución
NODE_ENV=production
